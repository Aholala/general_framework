# alg_ballistic

带二次阻力的弹道数值解算。输入目标相对距离、目标速度、弹速和系统延迟，输出 pitch 补偿、yaw 提前量与飞行时间。

目标纵向、横向和竖直速度都会参与一次提前量预测。高速机动目标仍应由视觉端提供经过时间同步的状态，不能用本算法掩盖时间戳延迟。

阻力系数与弹速必须现场标定；视觉坐标变换、云台机械零偏和发射许可属于模块或 App，不进入本算法。无解时显式返回 `NO_SOLUTION`，禁止继续使用旧解。
