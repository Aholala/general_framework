# bsp_storage

统一的非易失存储基类，覆盖片内 Flash、QSPI/OSPI NOR、EEPROM、SDMMC 块设备和主机文件模拟器。上层只依赖读、编程、擦除、同步和几何信息，不依赖具体存储控制器。

几何结构明确读/写对齐、擦除块大小、是否必须先擦除以及是否支持内存映射。驱动必须进行地址越界和对齐检查；上层不得假设字节可重复覆盖。需要持久化能力的上层模块可通过适配操作表使用本接口。

当前板图没有分配 QSPI 或 SDMMC 引脚，因此 STM32H723 端口暂不实例化存储对象；分配硬件后只需新增端口驱动，不修改模块。
