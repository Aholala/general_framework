# module_referee_ui

裁判客户端 UI 图形队列，支持直线、矩形、圆、椭圆、圆弧、数值和字符串，以及增、改、删操作。图形使用官方 15 字节位域格式编码，队列自动按 7/5/2/1 个图形组成合法批次并限频发送。

对象和队列由调用方静态提供，发送依赖 `module_referee_t`，不直接访问 USART。字符串最多 30 字节且单独发送；图形名称必须由项目保证三字节唯一。赛季协议变更时需核对交互命令 ID、客户端 ID 规则与位域定义。

UI 是非关键功能：发送失败只保留队列并返回错误，不得阻塞控制任务，也不得影响电机安全状态。
